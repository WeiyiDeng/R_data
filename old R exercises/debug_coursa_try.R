mean(w)
traceback()

debug(lm)
lm(y-x)                          # gets the browser in console, type "n" for each line until locate the problem

# "recover" is also a way to debug, see slides of R programming course by JHU in coursa, Lecture 3 Debugging